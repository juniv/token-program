/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link TokenData}
 * @category Accounts
 * @category generated
 */
export type TokenDataArgs = {
  name: string
  user: web3.PublicKey
  mint: web3.PublicKey
  earned: web3.PublicKey
  reserve: web3.PublicKey
  mintBump: number
  earnedBump: number
  reserveBump: number
  transactionFee: beet.bignum
  saleFee: beet.bignum
  discount: beet.bignum
  rewardGenericToken: beet.bignum
  rewardMerchantToken: beet.bignum
  rewardUsdcToken: beet.bignum
}

const tokenDataDiscriminator = [10, 136, 199, 13, 59, 103, 129, 70]
/**
 * Holds the data for the {@link TokenData} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class TokenData implements TokenDataArgs {
  private constructor(
    readonly name: string,
    readonly user: web3.PublicKey,
    readonly mint: web3.PublicKey,
    readonly earned: web3.PublicKey,
    readonly reserve: web3.PublicKey,
    readonly mintBump: number,
    readonly earnedBump: number,
    readonly reserveBump: number,
    readonly transactionFee: beet.bignum,
    readonly saleFee: beet.bignum,
    readonly discount: beet.bignum,
    readonly rewardGenericToken: beet.bignum,
    readonly rewardMerchantToken: beet.bignum,
    readonly rewardUsdcToken: beet.bignum
  ) {}

  /**
   * Creates a {@link TokenData} instance from the provided args.
   */
  static fromArgs(args: TokenDataArgs) {
    return new TokenData(
      args.name,
      args.user,
      args.mint,
      args.earned,
      args.reserve,
      args.mintBump,
      args.earnedBump,
      args.reserveBump,
      args.transactionFee,
      args.saleFee,
      args.discount,
      args.rewardGenericToken,
      args.rewardMerchantToken,
      args.rewardUsdcToken
    )
  }

  /**
   * Deserializes the {@link TokenData} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [TokenData, number] {
    return TokenData.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link TokenData} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey
  ): Promise<TokenData> {
    const accountInfo = await connection.getAccountInfo(address)
    if (accountInfo == null) {
      throw new Error(`Unable to find TokenData account at ${address}`)
    }
    return TokenData.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Deserializes the {@link TokenData} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [TokenData, number] {
    return tokenDataBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link TokenData} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return tokenDataBeet.serialize({
      accountDiscriminator: tokenDataDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link TokenData} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: TokenDataArgs) {
    const instance = TokenData.fromArgs(args)
    return tokenDataBeet.toFixedFromValue({
      accountDiscriminator: tokenDataDiscriminator,
      ...instance,
    }).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link TokenData} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: TokenDataArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      TokenData.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link TokenData} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      name: this.name,
      user: this.user.toBase58(),
      mint: this.mint.toBase58(),
      earned: this.earned.toBase58(),
      reserve: this.reserve.toBase58(),
      mintBump: this.mintBump,
      earnedBump: this.earnedBump,
      reserveBump: this.reserveBump,
      transactionFee: (() => {
        const x = <{ toNumber: () => number }>this.transactionFee
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      saleFee: (() => {
        const x = <{ toNumber: () => number }>this.saleFee
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      discount: (() => {
        const x = <{ toNumber: () => number }>this.discount
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      rewardGenericToken: (() => {
        const x = <{ toNumber: () => number }>this.rewardGenericToken
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      rewardMerchantToken: (() => {
        const x = <{ toNumber: () => number }>this.rewardMerchantToken
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      rewardUsdcToken: (() => {
        const x = <{ toNumber: () => number }>this.rewardUsdcToken
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const tokenDataBeet = new beet.FixableBeetStruct<
  TokenData,
  TokenDataArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['name', beet.utf8String],
    ['user', beetSolana.publicKey],
    ['mint', beetSolana.publicKey],
    ['earned', beetSolana.publicKey],
    ['reserve', beetSolana.publicKey],
    ['mintBump', beet.u8],
    ['earnedBump', beet.u8],
    ['reserveBump', beet.u8],
    ['transactionFee', beet.u64],
    ['saleFee', beet.u64],
    ['discount', beet.u64],
    ['rewardGenericToken', beet.u64],
    ['rewardMerchantToken', beet.u64],
    ['rewardUsdcToken', beet.u64],
  ],
  TokenData.fromArgs,
  'TokenData'
)
